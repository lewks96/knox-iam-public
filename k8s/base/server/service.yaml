apiVersion: v1
kind: Service
metadata:
  name: knox-server
  namespace: knox
  labels:
    app: knox-server
    app.kubernetes.io/part-of: knox-platform
spec:
  selector:
    app: knox-server
  ports:
    - port: 80
      targetPort: 8080
      name: http
  type: ClusterIP

