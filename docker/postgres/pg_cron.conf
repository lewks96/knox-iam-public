# pg_cron configuration — baked into the custom Knox Postgres image.
# Loaded via /etc/postgresql/conf.d/ include_dir.

shared_preload_libraries = 'pg_cron'

# pg_cron runs its metadata tables in this database.
# Must match the primary application database (see POSTGRES_DB in compose/k8s).
cron.database_name = 'knox'

